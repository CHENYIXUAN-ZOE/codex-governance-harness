# 0.2.0 受控安装记录

- 日期：2026-09-20
- 目标：`/Users/chenyi/.codex`
- Plugin：`codex-governance-harness@codex-governance-harness-local`
- 事务：`20260920T025054Z-a6e931ca`
- 状态：Installed / Healthy
- 授权：用户明确要求谨慎修改当前全局配置，并要求从重复纠正自动学习偏好；本次按此范围完成源码验证与部署。

## 部署前

0.1.0 的全局区块、active state 与缓存存在，对应 Plugin 和 marketplace 未注册。仅凭旧安装记录不能判为健康。

## 备份与写入

完整当前规则、config、active state 和旧事务记录备份于：

`/Users/chenyi/.codex/governance-harness/backups/20260920T025052Z-pre-0.2.0`

差异见 [全局规则 diff](0.2.0-global.diff)。安装器按已记录哈希和所有权清理旧残留，再安装 0.2.0；没有整体覆盖 config。Finder 元数据已明确移至临时备份，未打入 Plugin 包。

- 全局 AGENTS 与源码一致。
- 安装包 28 个文件与源码逐文件哈希一致。
- doctor 全部通过。
- 无关 config 字段解析后的值与安装前一致。
- 已安装 5 个治理技能，包括 `learn-user-preferences`。
- 一个真实全新只读任务确认加载 0.2.0 与 5 个技能。
- 使用实际偏好工具保存本次用户明确表达的专业判断偏好，revision 1；未捏造先前任务的反馈。运行时偏好不进入本源码仓库。

## 验证

43 项自动化测试、官方 Plugin/5 Skill 校验、源码结构和 Ruff 全部通过。隔离新任务验证包括明确偏好保存/检索，以及不显式要求保存的候选形成/重复纠正晋升/后续检索。详见 [验证记录](../evaluation/EVALUATION-0.2.0.md)。

## 回退

当前安装器的 `rollback` 默认为 dry-run；审阅后加 `--apply` 可撤回 0.2.0 拥有的受管区块和插件注册，保留无关用户编辑及运行时偏好库。

由于这是两阶段版本替换，要恢复部署前的 0.1.0 残留状态，还需在移除新版后按上方备份恢复旧 AGENTS 受管区块、active state 与旧事务记录。不要用旧 config 整体覆盖后续用户修改；优先保留当前无关配置。备份中保存了部署脚本、写入前哈希与部署结果，供核对和恢复。

回退不会忘记已保存的偏好；停用或撤销偏好通过独立学习工具进行。未执行真实环境二次回退，回退行为在隔离 HOME/CODEX_HOME 测试中验证。

## 下一门禁

观察真实使用中的检索遗漏、错误归纳和范围混淆；不将小样本验证当作永不遗忘的保证，也不自动追加新全局规则。
